# SMS v2 — Architecture

> Added fingerprint dedup + hit_count scoring. Three-tier not yet implemented.

Key features:
- Fingerprint-based dedup on write
- hit_count tracking for frequently accessed entries
- score-based ranking
